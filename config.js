const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "db4free.net",
    user: "kucingompong",
    password: "rewq0987",
    database: "reserve",
  },
  listPerPage: 10,
};
module.exports = config;